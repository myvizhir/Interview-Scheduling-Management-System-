function fnValidate(){
    // //password
    // let pas=document.regForm.password.value;
    // if (!(pas.match(/[a-z]/g) && pas.match(/[A-Z]/g) && 
    //     pas.match( /[0-9]/g) && pas.match(/[^a-zA-Z\d]/g) && pas.length >= 8))
    // {
    //     alert("Password should contain caps, small, numbers");
    //     document.regForm.pas.focus();
    //     return false;
    // }
    // return true;
}